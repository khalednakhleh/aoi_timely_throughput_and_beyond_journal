

P = 0.1

G = 0.8

TRANS = [1-P, P ;
         G, 1-G];

mc =  dtmc(TRANS, 'StateNames', ["ON", "OFF"]);


walk_in = simulate(mc, 10);

walk_in(walk_in == 2) = 0


other_walks = [];

mc =  dtmc(TRANS, 'StateNames', ["ON", "OFF"]);

for x = 1 : 10
    
    
    
end

%walk_in = walk_in(end);


graphplot(mc,'ColorEdges',true);